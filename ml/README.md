"""Learning code for the ML plan (docs/ml/). Not production.

Production ranking stays in app/model.py. This tree is for tiny datasets,
training experiments, and artifacts. Phases 1–8 live here; later phases add
sibling folders when those phases begin.
"""
